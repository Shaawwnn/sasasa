---
description: Sync README and guides with the current state of the code.
argument-hint: "[path]"
---

Use the **doc-updater** agent.

Generate from the code, never from memory. Flag documentation that contradicts the code rather than quietly rewriting around it.
